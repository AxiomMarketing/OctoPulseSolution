# E2E Test Report — API Integrations Pipeline

**Date:** 2026-04-15T00:00:00+00:00
**Tested by:** impl-e2e (APEX team)

## Results

| # | Test | Status | Output |
|---|------|--------|--------|
| 1 | vsearch meta-ads | FAIL | Embed server ConnectionRefused at localhost:8088 — remote embed model not running |
| 2 | validate valid | PASS | {"valid": true} |
| 3 | validate invalid | PASS | {"valid": false, "error": "'INVALID_VALUE' is not one of ['today', 'yesterday'...]", "path": "date_preset"} |
| 4 | rate-limit | PASS | OK: atlas_meta-ads 1/150 |
| 5 | recipe dry-run | PASS | curl -s 'https://graph.facebook.com/v21.0/act_XXXXXXXXX/insights?...' -H 'Authorization: Bearer <TOKEN>' |
| 6 | bw-get key | PASS | OK: key fetched (214 chars, starts with EAAco...) |
| 7 | real API call | PASS | {"id":"10235816715655568","name":"Jonathan Dewaele"} — OK: API responded successfully |

## Verdict

**PARTIAL PASS** — Tests 2-7 all pass (validation, rate-limit, recipe, secret fetch, real API call all healthy).

**Test 1 FAIL** — vsearch (ClawMem) fails because the remote embedding server at `localhost:8088` is not running on the VPS. The ClawMem binary is installed and the collection `docs-meta-ads` exists, but vector search requires the embed service to be active. This is an infrastructure/configuration issue, not a code issue.

## Action Required

- Start or configure the embedding server on port 8088 (e.g. Ollama or compatible embed endpoint) for ClawMem vsearch to work.
- Tests 2–7 confirm the core pipeline (schema validation, rate-limiting, recipe generation, secret management, and live Meta API connectivity) is fully operational.
